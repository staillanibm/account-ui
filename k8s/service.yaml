apiVersion: v1
kind: Service
metadata:
  name: account-ui-service
  namespace: account-ui
  labels:
    app: account-ui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: account-ui